layui.use(['element', 'layer', 'upload', 'form'], function() {
    var $ = layui.jquery,
        element = layui.element,
        layer = layui.layer,
        upload = layui.upload,
        form = layui.form;
})